services:
  web:
    build:
      context: ./site
      dockerfile: Dockerfile
      target: dev

    image: automato.cloud:dev

    environment:
      NODE_ENV: development

    volumes:
      - ./site:/app
      - /app/node_modules
      - /app/.next

    command: npm run dev

networks:
  automato:
    driver: bridge